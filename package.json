{
  "name": "nami-agendamento",
  "version": "1.0.0",
  "description": "Sistema de agendamento médico NAMI",
  "engines": {
    "node": "18.20.4"
  },
  "scripts": {
    "build": "cd server && rm -rf node_modules && rm -f package-lock.json && npm install",
    "start": "cd server && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "bcryptjs": "^2.4.3", 
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  }
}